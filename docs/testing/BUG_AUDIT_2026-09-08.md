# Bug audit — 8 September 2026

Source: `origin/main` at `cfdfc3f`, branch `cursor/bug-audit-fixes-aa9f`.

## Task packet

Player outcome: dual commercials never meet head-on on A1/A2; P assigns priority
crew to whichever commercial is at stand; follow/livery views survive respawn
reordering; approach shows gear down; night cabin frames stay dark; headless
domain tests keep compiling.

Scope: Simulation reservation / focus APIs, TrafficWaitMonitor, commercial view
sync, gear bias, cabin glow, ArtPresentationLoader UV/Addressables,
`scripts/dotnet-harness`, regression tests.

Acceptance: reproduce defects with probes before fixes; `scripts/test-domain.sh`
green afterwards; Unity EditMode still required before merge. Save schema,
economy balance and art asset IDs unchanged.

## Findings and fixes

| Severity | Defect | Fix |
| --- | --- | --- |
| High | Dual commercials occupied the same taxi segment for ~981s / 30k dual seconds; reservations did not gate phase progress. | Stall phase progress when `TryReplace` fails; require corridor for TaxiIn/TaxiOut; hold-short releases corridor at taxi-out end so Landing↔Takeoff cannot deadlock. |
| High | `EnablePriorityCrew` / delay HUD always targeted `_flights[0]`, so P no-op'd while the secondary was at stand. | `FocusFlight` prefers any commercial at stand. |
| High | Respawn re-sorted by `SpawnedAt` while visuals stayed on list indices → teleport / wrong follow target. | Remap commercial transforms by `AircraftId`. |
| High | Approach flew with gear retracted. | `GearBias(Approach)` deploys. |
| Medium | Departed flights kept the runway for the 6s reset window. | `Release` on Departed / IsComplete. |
| Medium | Wait monitor kept the first blocked resource forever. | Update resource, keep original start time. |
| Medium | `ApplyPresentationMaterials` preserved textured materials on UV-less meshes. | UV check before albedo preserve. |
| Medium | Addressables success path never cached/released handles. | Session prefab cache. |
| Medium | `AircraftAssetTests` (UnityEngine) broke `test-domain.sh`. | Exclude from harness like layout tests. |
| Low | Cabin window *frame* meshes matched night glow. | Exclude names containing `frame`. |

## Verification

- Probe before: `same_taxi_seg_seconds=981`, priority misses when secondary at stand,
  `departed_runway_hold_seconds=12`.
- Probe after: `same_taxi_seg_seconds=0`, priority succeeds on first eligible tick,
  `departed_runway_hold_seconds=0`, 368 dual cycles in 30k seconds.
- `scripts/test-domain.sh`: 118/118 (was 113 Domain/Simulation/Persistence mirror
  plus new regressions; Presentation Unity tests still Mac-only).
- No Unity editor in this environment — `scripts/test-unity.sh` still required
  before merging.
